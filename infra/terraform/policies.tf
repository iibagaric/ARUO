locals {
  recommended_policies = {
    require_tag_university = "Require tag university on resources"
    require_tag_student    = "Require tag student on resources"
    allowed_locations      = "Allow only Europe Azure regions"
  }
}

